type Query {
  allAlbums: [Album]
  album(id: ID!): Album
}

type Album {
  _id: ID!
  name: String
  released: Int
  artists: [Artist]
  bands: [Band]
  tracks: [Track]
  genres: [Genre]
  image: String
}
